<%= form_with(model: usuario) do |form| %>
  <% if usuario.errors.any? %>
    <div style="color: red">
      <h2>
        <%= pluralize(usuario.errors.count, "error" ) %> prohibited this usuario from being saved:
      </h2>

      <ul>
        <% usuario.errors.each do |error| %>
          <li>
            <%= error.full_message %>
          </li>
          <% end %>
      </ul>
    </div>
    <% end %>

      <div>
        <%= form.label :Nombre, style: "display: block" %>
          <%= form.text_field :nombre %>
      </div>

      <div>
        <%= form.label :edad, style: "display: block" %>
          <%= form.text_field :edad %>
      </div>

      <div>
        <%= form.label :Direccion, style: "display: block" %>
          <%= form.text_field :direccion %>
      </div>

      <div>
        <%= form.label :Telefono, style: "display: block" %>
          <%= form.text_field :telefono %>
      </div>

      <div>
        <br>
        <label for="usuario_trabajo_id"> Elige tu postulación: </label>
        <select name="usuario[trabajo_id]" id="usuario_trabajo_id">
          <% @trabajos.each do |trabajo| %>
            <option value="<%= trabajo.id %>">
              <%= trabajo.id %> - <%= trabajo.puesto%>
            </option>
            <%end%>
        </select>
      </div>

      <div>
        <br>
        <%= form.submit class:"btn btn-primary " %>
  </div>
<% end %>